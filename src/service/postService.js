const { addUser } = require("../db/mysql/querries")
const { generateToken } = require("../utils/crypt")


// const register = async (login, password) => {
//     addUser(login, generateToken(password))
// }

module.exports = {
    //register
}